# Place the real acquire.json here. The example template is in
# acquire.example.json; copy it and fill in the real share tokens
# and fragment secrets.
#
# This file is in .gitignore. Do not commit secrets.
